# Per-role runtime config — applies to BOTH software_dev_full and software_dev_fast.
#
# Tester is mostly Bash (run pytest → fix failures → commit). Trace data
# showed 90%+ Bash tool use with no deep-thinking benefit beyond reading the
# pytest output. Sonnet+low handles this fine.
model = "sonnet"
effort = "low"
